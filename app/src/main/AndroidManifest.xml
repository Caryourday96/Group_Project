<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alonquin.cst2335.group_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Switcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OCTranspo" />
        <activity android:name=".MovieInfo" />
        <activity android:name=".PIForm" />
        <activity android:name=".MCQuizCreator" />
        <activity android:name=".QuizActivity" />
        <activity
            android:name=".DisplayQuiz"
            android:label="@string/title_activity_display_quiz"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>